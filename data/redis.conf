unixsocket /var/run/redis.sock
unixsocketperm 600

# daemonize yes
#requirepass strong-password
# access only via unix socket
port 0
appendonly yes
# appendfsync always
maxmemory 50mb
# maxmemory-policy allkeys-lru